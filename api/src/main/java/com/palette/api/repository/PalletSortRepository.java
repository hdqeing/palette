package com.palette.api.repository;

import com.palette.api.model.PalletSort;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PalletSortRepository extends JpaRepository<PalletSort, Long> {
}
